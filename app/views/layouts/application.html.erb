<!DOCTYPE html>
<html>
<head>
  <title>FeedBee</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <!-- Standard Meta -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
</head>
<body class="fullscreen">
<div class="ui fixed inverted large menu">
    <a class="large item">FeedBee</a>
    <div class="right menu">
        <div id='nav-search' class = 'item'>
            <div class="ui icon mini input">
                <input id="nav-search" type="text" placeholder="Search for something!">
                <i id="bot-pad-zero" class="search icon"></i>
            </div>
        </div>
      <% if current_user %>
        <%= link_to "Hello, #{current_user.name}", "#", class: "item" %>
        <%= link_to "Log Out", logout_path, class: "item" %>
      <% else %>
        <%= link_to "Log In with Facebook", login_path, class: "item" %>
        <a class="item">Sign Up</a>
      <% end %>
    </div>
</div>

<%= yield %>

</body>
<div class="ui inverted vertical footer segment">
    <div class="ui center aligned container">
      <div class="ui horizontal inverted small divided link list">
        <a class="item" href="#">Site Map</a>
        <a class="item" href="#">Contact Us</a>
        <a class="item" href="#">Terms and Conditions</a>
        <a class="item" href="#">Privacy Policy</a>
      </div>
    </div>
  </div>
</html>
